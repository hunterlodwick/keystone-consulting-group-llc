# Services overhaul

Build only web-design and automations into deep service pages. Preserve their hero copy; explain software fees separately because the inherited AI tagline says free. Keep all nine shallow entries and their renderer unchanged. Optional typed fields select the deep renderer. Existing fonts, theme tokens and images; no text overlays or animation gating.

1. Capture before screenshots and rendered text at 1440/390, light/dark; baseline nine other routes.
2. Expand four capabilities per page, pain, sourced research with explicit derived arithmetic, process, fit, seven FAQs and ContactForm close.
3. Run build/typecheck, browser matrix, regression comparison, source checks and SlopMonster on full rendered body text.
4. Hermes verifies read-only, Astra reviews; at most two correction cycles. Save all evidence here. Leave local preview running. No deploy.

Research is context, never a KCG result. Use only factsheet figures. Unknown client savings remain [needs number]. Computations use explicit assumptions and unit conversions. Preserve existing unrelated working-tree files.

Final isolation: another process added SERVICE_CTAS to the shared file and rebuilt its dist during this task. Preserve that work. Review the requested original-scope implementation in /tmp/kcg-services-overhaul-review at port 4177. Frozen overhaul-only source and separate concurrent delta are stored here; owner choice is pending.
